{
  "name": "badge-light",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:itch": "vite build --mode itch",
    "build:crazygames": "vite build --mode crazygames",
    "build:kongregate": "vite build --mode kongregate",
    "build:portals": "npm run build:itch && npm run build:crazygames && npm run build:kongregate",
    "preview": "vite preview"
  },
  "dependencies": {
    "phaser": "^3.80.1"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "vite-plugin-singlefile": "^2.3.3"
  }
}
